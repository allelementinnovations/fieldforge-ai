{
  "name": "fieldforge-ai",
  "version": "2.0.0",
  "private": true,
  "type": "commonjs",
  "description": "A browser-based workflow that turns hands-on work problems into structured GPT-5.6 briefs and practical next steps.",
  "scripts": {
    "test": "node --test",
    "serve": "python3 -m http.server 8000"
  },
  "engines": {
    "node": ">=20"
  }
}
